name: Compilació multiplataforma

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Instal·la dependències
        run: |
          pip install pyinstaller weasyprint markdown
          sudo apt-get update
          sudo apt-get install -y libpango-1.0-0 libpangoft2-1.0-0

      - name: Compila cada programa (Linux)
        run: |
          mkdir -p dist_linux

          # Processa cada carpeta de primer nivell
          for carpeta_programa in */; do
            carpeta_programa="${carpeta_programa%/}"  # Elimina la barra final

            # Ignora carpetes del sistema
            case "$carpeta_programa" in
              .github|.git|assets|docs) continue ;;
            esac

            # Detecta source/ o Source/
            if [[ -d "${carpeta_programa}/source" ]]; then
              source_dir="${carpeta_programa}/source"
            elif [[ -d "${carpeta_programa}/Source" ]]; then
              source_dir="${carpeta_programa}/Source"
            else
              echo "⚠️  Saltant '${carpeta_programa}': no conté 'source' ni 'Source'"
              continue
            fi

            # Normalitza el nom de l'executable: minúscules + guions
            nom_executable=$(echo "$carpeta_programa" | tr '[:upper:]' '[:lower:]' | tr ' ' '-' | tr '_' '-')

            # Troba el primer fitxer .py al directori source
            fitxer_py=$(find "$source_dir" -maxdepth 1 -name "*.py" -type f | head -n1)

            if [[ -z "$fitxer_py" ]]; then
              echo "⚠️  Saltant '${carpeta_programa}': cap fitxer .py a ${source_dir}"
              continue
            fi

            echo "📦 ${carpeta_programa} → ${nom_executable}"

            # Compila amb PyInstaller
            cd "$source_dir"
            pyinstaller --onefile "$(basename "$fitxer_py")" --name "$nom_executable" --distpath "../../dist_linux" --log-level=WARN
            cd ../..

            # Converteix el manual a PDF
            if [[ -f "${carpeta_programa}/MANUAL.md" ]]; then
              echo "📄 Convertint MANUAL.md a PDF..."
              python3 << 'PYTHON_SCRIPT'
import markdown
from weasyprint import HTML, CSS
import sys

md_file = "${carpeta_programa}/MANUAL.md"
pdf_file = "dist_linux/${nom_executable}_MANUAL.pdf"

with open(md_file, 'r', encoding='utf-8') as f:
    md_content = f.read()

html_content = markdown.markdown(md_content, extensions=['extra', 'codehilite'])

html_template = f"""
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body {{
            font-family: Arial, sans-serif;
            line-height: 1.6;
            margin: 2cm;
            color: #333;
        }}
        h1, h2, h3 {{
            color: #2c3e50;
        }}
        code {{
            background-color: #f4f4f4;
            padding: 2px 6px;
            border-radius: 3px;
            font-family: monospace;
        }}
        pre {{
            background-color: #f4f4f4;
            padding: 10px;
            border-radius: 5px;
            overflow-x: auto;
        }}
    </style>
</head>
<body>
{html_content}
</body>
</html>
"""

HTML(string=html_template).write_pdf(pdf_file)
print(f"✅ PDF creat: {pdf_file}")
PYTHON_SCRIPT
            elif [[ -f "${carpeta_programa}/MANUAL_US.md" ]]; then
              echo "📄 Convertint MANUAL_US.md a PDF..."
              python3 << 'PYTHON_SCRIPT'
import markdown
from weasyprint import HTML, CSS

md_file = "${carpeta_programa}/MANUAL_US.md"
pdf_file = "dist_linux/${nom_executable}_MANUAL.pdf"

with open(md_file, 'r', encoding='utf-8') as f:
    md_content = f.read()

html_content = markdown.markdown(md_content, extensions=['extra', 'codehilite'])

html_template = f"""
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body {{
            font-family: Arial, sans-serif;
            line-height: 1.6;
            margin: 2cm;
            color: #333;
        }}
        h1, h2, h3 {{
            color: #2c3e50;
        }}
        code {{
            background-color: #f4f4f4;
            padding: 2px 6px;
            border-radius: 3px;
            font-family: monospace;
        }}
        pre {{
            background-color: #f4f4f4;
            padding: 10px;
            border-radius: 5px;
            overflow-x: auto;
        }}
    </style>
</head>
<body>
{html_content}
</body>
</html>
"""

HTML(string=html_template).write_pdf(pdf_file)
print(f"✅ PDF creat: {pdf_file}")
PYTHON_SCRIPT
            fi

            echo "✅ ${nom_executable} compilat"
          done
        shell: bash

      - name: Puja a Release (Linux)
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist_linux/*
            !dist_linux/*.spec
          tag_name: ${{ github.ref_name }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Instal·la dependències
        run: |
          pip install pyinstaller weasyprint markdown
        shell: pwsh

      - name: Compila cada programa (Windows)
        run: |
          New-Item -Path "dist_windows" -ItemType Directory -Force | Out-Null

          # Processa cada carpeta de primer nivell
          Get-ChildItem -Directory | ForEach-Object {
            $carpeta_programa = $_.Name

            # Ignora carpetes del sistema
            if ($carpeta_programa -in @(".github", ".git", "assets", "docs")) {
              return
            }

            # Detecta source/ o Source/
            $source_dir = $null
            if (Test-Path "${carpeta_programa}/source") {
              $source_dir = "${carpeta_programa}/source"
            } elseif (Test-Path "${carpeta_programa}/Source") {
              $source_dir = "${carpeta_programa}/Source"
            } else {
              Write-Host "⚠️  Saltant '${carpeta_programa}': no conté 'source' ni 'Source'"
              return
            }

            # Normalitza el nom de l'executable
            $nom_executable = $carpeta_programa.ToLower() -replace ' ', '-' -replace '_', '-'

            # Troba el primer fitxer .py
            $fitxer_py = Get-ChildItem "${source_dir}/*.py" -File | Select-Object -First 1

            if (-not $fitxer_py) {
              Write-Host "⚠️  Saltant '${carpeta_programa}': cap fitxer .py a ${source_dir}"
              return
            }

            Write-Host "📦 ${carpeta_programa} → ${nom_executable}.exe"

            # Compila amb PyInstaller
            Set-Location $source_dir
            pyinstaller --onefile $fitxer_py.Name --name $nom_executable --distpath "..\..\dist_windows" --log-level=WARN
            Set-Location ..\..

            Write-Host "✅ ${nom_executable}.exe compilat"
          }
        shell: pwsh

      - name: Puja a Release (Windows)
        uses: softprops/action-gh-release@v2
        with:
          files: dist_windows/*.exe
          tag_name: ${{ github.ref_name }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
